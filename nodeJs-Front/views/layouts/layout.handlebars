
<!DOCTYPE html>
<html>
   <head>
      <meta charset="utf-8">
      <title>Login App</title>
      <link rel="stylesheet" href="/css/bootstrap.css" />


   </head>
   <body>
      <div class="container">
      <div class="header clearfix">
        <nav id="nav">
          <ul class="nav nav-pills pull-right">
            {{#if user}}
               <li role="presentation"><a href="/">Bienvenido 
               </a></li>
               <input type="hidden" id="userstr" value="{{user}}"/>
               <li role="presentation"><a href="/users/gameSala">Iniciar Juego</a></li>
               <li role="presentation"><a href="/users/logout">Cerrar Sesión</a></li>
            {{else}}
               <li role="presentation"><a href="/users/login">Iniciar Sesión</a></li>
               <li role="presentation"><a href="/users/register">Resgistro</a></li>
                
            {{/if}}
          </ul>
        </nav>
        <h4 class="text-muted">Bingo El Gran Buda</h4>
      </div>
 </div>
      <div class="row">
        <div class="col-lg-12">
            
           {{#if success_msg}}
              <div class="alert alert-sucess">{{sucess_msg}}</div>
            {{/if}}
            {{#if error_msg}}
               <div class="alert alert-danger">{{error_msg}}</div>
             {{/if}}
             {{#if error}}
               <div class="alert alert-danger">{{error}}</div>
             {{/if}}
          {{{body}}}
          </div>
        </div>
    <div>
     <footer class="footer">
        <center><p> &copy; 2022 || Bingo El Gran Buda by Adryan Ynfante </p></center>
      </footer>
   </div>
   </body>
</html>
<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>